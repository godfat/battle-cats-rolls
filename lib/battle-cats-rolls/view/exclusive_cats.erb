<div id="exclusive">
  <% if arg[:exclusive_cats].any? %>
    <p>Next exclusive festival cats:</p>
  <% end %>
  <ul>
    <% arg[:exclusive_cats].each do |(cat, a_or_b)| %>
      <li><%= "#{link_to_roll(cat)}: #{cat.sequence}#{(65 + a_or_b).chr}" %></li>
    <% end %>
  </ul>
</div>
